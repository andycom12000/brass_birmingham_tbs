{
  "permissions": {
    "allow": [
      "Bash(selene:*)",
      "Bash(bash scripts/lint.sh:*)",
      "Bash(python:*)"
    ]
  },
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Write|Edit",
        "hooks": [
          {
            "type": "command",
            "command": "python -c \"import sys,json;d=json.load(sys.stdin);p=d.get('tool_input',{}).get('file_path','');exit(0 if p.endswith('.lua') else 1)\" && bash scripts/lint.sh 2>&1 || true",
            "timeout": 30,
            "statusMessage": "Linting Lua files..."
          }
        ]
      }
    ]
  }
}
